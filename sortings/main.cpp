#include <iostream>
#include <vector>

#include "fillTheVector.h"
#include "Sortings.h"
#include "printVector.h"
#include "writeToFile.h"

int main() {
    std::string filePath = "File.txt";

    /*std::vector <int> buble;
    fillTheVector(buble);
    BubbleSort(buble);
    writeToFile(filePath, buble);
    printVector(buble);

    std::vector <int> insert;
    fillTheVector(insert);
    insertionSort(insert);
    writeToFile(filePath, insert);
    printVector(insert);

    std::vector <int> shell;
    fillTheVector(shell);
    insertionSort(shell);
    writeToFile(filePath, shell);
    printVector(shell);

    std::vector <int> oddEven;
    fillTheVector(oddEven);
    insertionSort(oddEven);
    writeToFile(filePath, oddEven);
    printVector(oddEven);

    std::vector <int> cocktail;
    fillTheVector(cocktail);
    insertionSort(cocktail);
    writeToFile(filePath, cocktail);
    printVector(cocktail);

    std::vector <int> selection;
    fillTheVector(selection);
    insertionSort(selection);
    writeToFile(filePath, selection);
    printVector(selection);

    std::vector <int> comb;
    fillTheVector(comb);
    insertionSort(comb);
    writeToFile(filePath, comb);
    printVector(comb);*/

    std::vector <int> doubleSelection;
    fillTheVector(doubleSelection);
    insertionSort(doubleSelection);
    writeToFile(filePath, doubleSelection);
    printVector(doubleSelection);
}