#ifndef SORTINGS_SORTINGS_H
#define SORTINGS_SORTINGS_H

#include <vector>

void BubbleSort (std::vector <int>& vector);
void insertionSort (std::vector <int>& vector);
void shellSort (std::vector <int>& vector);
void oddEvenSort (std::vector <int>& vector);
void cocktailSort (std::vector <int>& vector);
void selectionSort (std::vector <int>& vector);
void combSort (std::vector <int>& vector);
void doubleSelectionSort (std::vector <int>& vector);
void quickSort (std::vector <int>& vector);

#endif //SORTINGS_SORTINGS_H